import { Title } from "@mantine/core";
import { motion } from "framer-motion";

const TitleSliderElement = ({ slide }) => {
  return <Title sx={{ fontSize: "10rem" }}></Title>;
};

export default TitleSliderElement;
