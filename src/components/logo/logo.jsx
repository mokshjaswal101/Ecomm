import { Box } from "@mantine/core";

const Logo = () => {
  return (
    <Box
      sx={{
        color: "white",
        fontWeight: "bold",
        fontSize: "3rem",
        letterSpacing: ".35rem",
      }}
    >
      ECOMM
    </Box>
  );
};

export default Logo;
